# 实验管理规范

## 已有基线

experiments/2026-09-16_baseline/raw/ 保存整理时 outputs/ 全部117个普通文件的原样副本，包括成功、失败、截图、模型与数据。manifest.json包含原路径、大小、SHA-256；原outputs保留以兼容旧命令。重复副本用于冻结证据，不代表新增实验。

- reference_learning：合成教师数据、线性policy和rollout。
- verified_poses：有用户确认或完整反馈报告的姿态相关记录；同类失败报告仍保留，需读result字段。
- commissioning_and_failures：串口、原生USB试验及中止记录，不能整体视为成功。
- studio_protocol：Studio抓取、IL、Transform和连接失败记录。
- visual_validation：模型图片与GIF（不是实机录像）。
- machine_specific_usb：旧机器注册表备份与修复结果；仅本机历史证据。

## 新实验

从experiments/_template复制到YYYY-MM-DD_short-name，填写run.json和README。每次运行使用唯一目录，保留命令、环境、配置、随机种子、数据划分、模型哈希、训练/推理耗时、显存、结果和失败原因。不要覆盖baseline或同名权重。

真实数据至少说明单位、坐标系、控制频率、时间戳来源、实际使用硬件和操作是否有监督。用户肉眼确认、固件反馈到位、独立测量精度分别记录。

## 校验

`python tools/maintenance/verify_evidence.py` 只校验归档文件，不打开硬件。
后续如需论文复现，必须单独定义指标、测试集和重复次数；本项目的冒烟测试不等于科研性能评估。

## 索引（2026-09-22 更新，供换机器/新会话快速定位，不改动下列目录本身）

按目录名的日期排列。**不要删除或改写下表指向的任何目录**——本表只是导航，真正的记录以各目录自己的 README 为准。

| 目录 | 内容 | 等级 | 状态 |
| --- | --- | --- | :-- |
| `_template` | 空模板，新实验从这里复制 | — | 模板 |
| `2026-09-16_baseline` | 迁移前既有基线：117 个历史文件 + manifest（SHA-256），含成功与失败记录 | L1/L3 混合 | 冻结基线，不再变动 |
| `2026-09-18_handoff` | 交接整理验证：35 项软件测试 + 117 历史文件哈希 + 11,742 份归档哈希 + 离线渲染 | L1 | 已完成 |
| `2026-09-18_m0_baseline` | M0 发布前的文件清单 + 哈希 + git log 快照 | — | 归档快照 |
| `2026-09-18_workspace_migration` | 主工作区迁移到 `D:\VLA`，747 个文件 SHA-256 逐一比对 | L1 | 已完成，历史记录 |
| `2026-09-19_windows_env` | 新开发机（desktop-m51oshe）环境重建 + 两条既有 L1 结论复核 | L1 | 已完成 |
| `2026-09-20_static_torque` | 静力矩对比：夹 1 kg 砝码 vs 抹刀压墙 | L1 | 已完成，供参考 |
| `2026-09-20_v2_model` | 仿真切换到 Dummy V2 模型（固件 DH、正运动学移植） | L1 | 已完成，是此后所有仿真的基础模型 |
| `2026-09-20_wall_sim_chain` | 墙面抹涂仿真链路首次打通（场景/控制/任务/示教/误差补偿） | L1 | 已完成，部分结论被下面两条取代 |
| `2026-09-20_wall_workspace` | 墙面可达区域分析（发现调试范围内几乎不可达） | L1 | 已完成，**M3 逐轴标定后需要重算** |
| `2026-09-20_rigid_tool` | 末端改为刚性抹刀 + J6 减速器 + 3D 打印刀座（去掉早期弹簧滑轨假设） | L1 | 已完成，是当前末端模型 |
| `2026-09-21_plaster_session_rl` | 抹涂手法强化学习（高度场代理材料，"贴墙先立起"手法） | L1 | 已完成，材料模型被 `2026-09-22_wall_cycle_rl` 取代 |
| `2026-09-21_ppo_visualization` | 上一版 RL 模型的浏览器可视化（折线回放） | L1 | 已完成，可视化方式被 `2026-09-22_wall_cycle_v03` 的 MuJoCo 联合仿真取代 |
| `2026-09-22_wall_cycle_rl` | 整片连续强化学习 v0.2：扫描 → 决策 → 装料/复用 → 连续施工 → 回位 → 重扫 | L1 | 已完成，被 v0.3 取代但保留对比；折线回放仍可用（`interactive_replay_v6`） |
| `2026-09-22_wall_cycle_v03` | 整片连续强化学习 v0.3：物理化砂浆（无角度规则）+ 真实 Dummy V2 机械臂 MuJoCo 联合仿真 | L1 | **当前进度，后续开发从这里继续**，见该目录 README 的「局限」「下一步」 |
| `2026-09-22_windows_env` | 空目录，无 README、无 run.json、`raw/` 也是空的 | — | 疑似未完成会话的残留，不是有效记录；未经用户同意不会删除，仅作提示 |

与 `2026-09-22_wall_cycle_v03` 平行的更详细的版本间差异记录见
`docs/changes/2026-09-22_wall_cycle_v0.3.md`（每一项改动的原因、内容、"为什么不能改回去"）。
