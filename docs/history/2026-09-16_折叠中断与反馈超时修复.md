# 折叠未完成与末端姿态待核对

用户截图显示折叠第3阶段，J3反馈约122.24°，当前轨迹目标约122.81°，反馈年龄15.64秒。后续窗口显示155秒以上无更新。日志最后发送目标为122.81°，未发送到折叠终点180°，因此截图并不是折叠完成姿态。用户进一步确认实体和模型的末端均不对；不能只改可视化偏移。

已查到旧 Fibre 客户端 USB OUT 写入使用 timeout=0（无限等待），接收剩余期限归零时也会传入0；应答重试默认5秒×5次。它们会使后台长时间阻塞，停止请求得不到及时处理。尚无线程堆栈证明本次阻塞发生在哪一个系统调用，不将代码风险直接冒充唯一根因。

已修复：

- USB OUT写入200ms有限超时；接收期限已过直接超时，不把0交给libusb。
- 接口发现完成后将角度读取通道应答等待设为150ms、最多2次尝试。
- 六轴读数超过600ms不作为有效姿态。
- 界面检测750ms以上无新反馈时取消原生通道、请求停止、取消待执行姿态，并禁用姿态按钮。

旧窗口停止请求未生效；按已核对进程路径结束本项目旧进程后，COM6仍连续返回拒绝访问。STOP未能送达，已告知用户先确认实体停稳，并拔插USB数据线释放占用。没有因为图形不正确而改写零点、限位或增加J5/J6偏移。

待完成：重新连接后读取实际角度，确认STOP应答；再验证折叠和直立按钮完整到位。若到达 `[0,-75,180,0,0,0]` 或 `[0,0,90,0,0,0]` 后实体末端仍偏，需对照原Studio动作和实体姿态定位目标定义/机械零位问题。本文件不表示末端姿态已修正。

## 重启后实机复测

用户重启后授权再次测试。COM6恢复，STOP收到应答，串口与原生反馈一致。使用GUI相同LiveController、5°/秒上位机目标速度执行一次直立→折叠，两段均完成，未重现J3约122°卡住：

- 直立反馈 `[0,-0.00455,89.99410,0,0,-0.00070]`，相对目标最大差值0.00590°。
- 折叠反馈 `[0,-74.99972,179.99395,0,0,0]`，相对目标最大差值0.00605°。
- 控制线程正常退出；记录位于 `outputs/preset_recheck_1789550272034353800.report.json`，原始命令与反馈在同名前缀日志。
- 35项软件测试通过。

这些是控制器反馈误差，不能视为实体末端空间精度，也不能单凭一次成功确定此前唯一根因。实体末端外观/方向仍待用户对照确认；未改动机械零位、预设目标或模型偏移。

## 用户确认正确实体姿态后的只读采集

用户提供正确折叠参考图，并明确实体已停在正确末端姿态、Studio已断开。只读采集得到串口角度 `[0,-75,180,0,0,0]`，原生USB连续5帧一致（J2约−74.99972°，J1浮动小于0.001°）。原始记录：`outputs/correct_fold_capture.json` 和 `outputs/capture_correct_fold_serial.jsonl`；未发送运动指令。

因此当前折叠目标与用户确认的正确实体姿态一致，保留原目标，不添加J5/J6补偿，不修改零点。此前J3≈122°截图属于动作未完成，最新完整复测终点已与本次正确姿态读数一致。直立末端是否符合用户要求仍需单独核对，不能由本次折叠确认推定。
