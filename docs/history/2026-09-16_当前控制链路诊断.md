# 2026-09-16 当前实机控制诊断

## 后续：用户确认 J1 实体运动，折叠指令反馈到位

用户确认上一轮实体机械臂正常运动，并要求执行折叠。新脚本 `tools/commission_fold_once.py` 按捕获的 Studio 顺序，从 `[0,0,90,0,0,0]` 先到 `[0,-75,90,0,0,0]`，再到 `[0,-75,180,0,0,0]`；每段要求连续四次反馈误差小于 0.15°，并检查路径范围及停滞。未修改速度、电流、零位和限位。

执行记录：`outputs/fold_once_1789546366924378600.json`，原始串口记录：`outputs/fold_serial.jsonl`。两段均反馈到位，最终约 `[0,-74.999,179.978,0,0,0]`，收到 STOP 回复。机械臂留在折叠目标处，没有自动展开。折叠实体表现待用户现场确认；J1 实体往返已获用户确认。

## 最新进展：重新摆正初始位置后，J1 往返反馈到位

用户报告发现原初始摆放使 J1 坐标与机械位置不一致，正向运动撞到机械限位；重新执行复位并在 Studio 操作到约 10.21°。捕获记录实际目标是 10.22°，最后又在 17:08:09 发送了 `[0,0,90,0,0,0]`。测试前独立 ASCII 和原生接口均读到这个复位姿态，未把用户较早的 10.21° 描述直接当作当前角度。

随后完成两段独立控制测试（Studio 未运行）：

| 段 | 目标入口 | J1 反馈过程（度） | 结果记录 |
|---|---|---|---|
| 去程 0→10 | 原生 `robot.move_j`，串口 START | 0→2.50→5.51→8.18→9.996→10.001 | `outputs/native_j1_1789546152444180900.json` |
| 回程 10→0 | Studio 同格式 ASCII 六角度命令，原生读取反馈 | 10.001→7.658→4.718→1.969→0.002→约0 | `outputs/native_j1_1789546220803937900.json` |

两段都要求连续四个反馈样本距目标小于 0.15°，其余五轴保持复位附近；结束均收到 STOP 回复。没有调整电流、限位、主控速度、零位或写入校准参数。测试最终留在 J1≈0°、J2≈0°、J3≈90°、其余轴≈0°。

这支持用户发现的初始位置/机械限位原因，证明正确交接姿态下两个目标入口均能产生连续、到位的反馈，而不是只能收到通信 ACK。不能据此反推已查明所有固件版本差异，也不能将反馈误差当作实体绝对精度。已询问用户实体是否明显转动，外部视觉确认待用户回复。其他关节、自动回零、连续算法控制及反馈时间戳/watchdog 尚未验收；`send_action` 的自动执行保护保持不变。

`tools/commission_native_j1.py` 新增 `--return-to-zero`（只允许从 J1=10±0.2°、其他轴复位附近返回）与 `--ascii-target`。默认不带 `--execute` 时只做预检。低层 `--direct-motor` 仍禁用。

## 最新结果（原生接口修复后；覆盖下方历史状态）

以下是重新摆正初始位置前的历史结果；已被上方 J1 往返测试更新，但保留排查证据。

1. 用户确认重启后速度恢复，并用 Studio 复位。随后独立接口读到 `[0,0,90,0,0,0]`，本次交接未发现开串口使角度回到折叠初值。
2. 管理员修复已成功：只给原生 MI_02 接口补充 `DeviceInterfaceGUIDs` 并重新加载该接口。未替换 COM6 驱动，未刷固件。记录 `outputs/native_interface_repair_result.json`，备份 `outputs/native_interface_registry_backup.json`，回退使用 `tools/repair_native_interface.ps1 -Mode Rollback`（需管理员）。
3. 已能用原生 Fibre 读取序列号 `325F368F3135`、接口定义和角度属性。接口定义保存到 `outputs/native_robot_interface.json`。没有固件版本、目标角度或电机故障状态的只读接口，无法由接口名确定实际固件实现。
4. 原生 `move_j` 对 J1=10° 两次返回 true，反馈约从 0.94° 到 1.90° 后停滞，其他轴保持在复位附近。触发停滞退出并收到 STOP 回复。证据：`outputs/native_j1_1789544720285375300.json`。这表明目标被固件接受，但不证明到达，也不能把问题仅归咎于 ASCII 格式或 ASCII 位置查询。
5. 一次绕过主控轨迹的低层小范围诊断采用档案 J1 30:1、反向换算；期间缓存停滞，之后读取到 J1≈−1.1447°，与此前 1.908° 及采用的保持换算不一致。该试验写入了低层位置和第二参数 0.2（档案解释为电机速度），不能描述为完全未写速度参数。低层函数实际单位和方向尚未证实，不能将 RPC 返回解释为物理保持成功。已在脚本参数入口禁用 `--direct-motor`，不继续使用该换算。
6. 原生角度与 ASCII 角度的偏置为 `[0,-75,180,0,0,0]`，档案零位是 `[0,-73,180,0,0,0]`。原作者公开主分支 J1 减速比是 50:1，本地 V2 档案是 30:1。这证明资料存在版本差异，但不能据此认定实机就是原版，也不能证明这是高层 10° 指令停在 1.9° 的原因。
7. 主动广播和逐轴请求位置后读数不变，没有获得支持“只需刷新缓存即可解决”的证据；当前接口也没有反馈时间戳，所以不能证明反馈持续新鲜。

新增 `tools/native_robot_diagnostic.py --watch-seconds 180`：只读取原生缓存角度，可在 Studio 独占 COM6 时记录；不发送运动或 CAN 刷新命令。已请求用户在带通信记录的 Studio 副本中完成一次实体确认到位的 J1=10° 对照，以比较实际成功时的指令序列与固件反馈。该对照尚待完成。

原作者源码依据：<https://github.com/peng-zhihui/Dummy-Robot/blob/main/2.Firmware/Core-STM32F4-fw/Robot/instances/dummy_robot.cpp>。仅做版本比较，未用于替换或烧录实机固件。

## 历史排查记录

## 已有证据

- 用户确认实体仍处于收拢/折叠姿态；六轴重复读取稳定，约 `[0.30,-72.71,178.13,1.56,-1.44,-0.01]` 度。不能用 Studio 的模型角度代替实机读数。
- 诊断 Studio 与原版同时运行时出现串口 `Open Error`；仅运行诊断副本后，记录到了 `!START\r\n` / `Started ok\r\n`，以及六角度加尾逗号、LF 的目标命令与队列应答。
- 收到队列应答和 `ok` 不代表完成运动；源码也会在目标未通过角度校验时回复 `ok`。
- 用户曾确认原版可带动实体，但本轮对明显运动的观察尚未得到肯定结论。

## 有界单轴测试

仅改变 J1 目标，其他五轴保持读取值；未更改电流、扭矩、硬件限位或驱动保护设置。

1. 不重新 START，J1 目标从 0.30 到 10.30 度，速度参数 3：3 秒内反馈不变，发送 STOP 并收到确认。
2. 预装相同目标后 START：反馈从 0.30 连续增加到约 1.80 度，再停滞；发送 STOP 并收到确认。其他五轴反馈未变。
3. 预装目标、START 后再发送一次相同目标，J1 从约 1.81 到 11.81 度：反馈仅约 1.76–1.81 波动，无有效推进，发送 STOP 并收到确认。

第三轮记录：`outputs/j1_10deg_1789542678409343000_result.json`。原始收发记录：`outputs/j1_once_serial.jsonl`。Studio 捕获：`outputs/studio_wire_capture.tsv`。

用户观察驱动板灯常亮，未报告故障代码。此现象不能单独排除驱动故障。

## 未解决

通信双向收发成立；目标到达、实体与反馈的一致性尚未验证。固定约 1.8 度的停点原因未确定。源码未发现相应的 J1 软件角度限幅，但不能据此排除实际烧录固件差异、驱动状态、反馈或传动问题。

不把反馈角度变化写成实体运动成功，不继续提高力矩、绕过保护或重复增大目标。下一步需要原版在当前硬件状态下的明确实体运动对照；如果原版也不能明显运动，应转向硬件/驱动状态检查。如果原版可以，则捕获这次确实成功的完整操作并逐项比较。

## 后续：Studio 可运动，但速度明显变慢

用户随后明确报告：原版仍能到各个角度，但复位、运动均变慢。独立测试曾写入速度参数 1 和 3；七字段角度命令调用 SetJointSpeed 后值保留在控制器 RAM，六字段 Studio 滑块命令不更新速度。因此之前测试造成的速度状态残留是直接相关的原因，不能将慢速归因于机械故障。档案源码 DEFAULT_JOINT_SPEED=30，Init 会重新设置该值；主程序未提供通过 ASCII 单独设置速度的命令。

为避免附带运动目标，尝试原生 Fibre 的 robot.set_joint_speed。系统存在 REF 1.0 Native Interface，WinUSB 服务已绑定，但 PyUSB/libusb 打开返回 Entity not found，设备注册项未读到 DeviceInterfaceGUIDs。没有修改系统驱动或注册表，没有执行任何原生写参数调用，速度尚未恢复。

原生诊断工具 tools/native_robot_diagnostic.py 当前不可宣称可用。仅在运行时接口表、实机序列号和速度函数签名校验通过后才允许 --restore-speed 写入。复制的旧客户端关闭了 CDC 回退和设备忙时自动 reset，并修复非交互控制台兼容问题。

用户随后确认重启后速度已恢复。此后只读查询，没有再次写入速度或目标。tools/probe_studio_transport.py 使用 Studio 原 spap.dll、同场景配置及 DTR 设置读取三次，与 pyserial 结果相同，均为 [0,-75,180,0,0,0]；这不能证明开口不会改变状态，但未发现两个库的当前角度读数差异。spapGetRTS 的底层返回值是非零整数，未将该值误当成精确的 Windows DCB 配置。

下一次受控交接要求用户先确认实体确实处于 Studio 已成功到达的竖直复位姿态，再关闭 Studio。检查独立查询是否仍是复位角度；若退回初始化值，优先查初始化/连接状态，不能直接发送动作。速度保持不写入。

最新只读角度为 [0,-75,180,0,0,0]，与档案源码 J2 下限 -73 不一致，实际固件版本和限位仍需核对。未为恢复速度扩大测试限位或发送新的运动目标。已请求用户在支撑和停稳状态下重启控制器，清除本轮 RAM 速度状态，然后只读复核。
