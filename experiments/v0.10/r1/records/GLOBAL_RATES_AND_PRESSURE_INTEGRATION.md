# 全局速度重测失败与v8压力集成初测

L1软件会话，未启动PPO。旧v4扩大审计仍运行，不改其核心源码。

## 全局速度修正没有解决原失败

上一轮 adaptive_coupled_boundary_002 的128/256档均失败，进程已退出。此次新版本 boundary_quadrature.adaptive_support.v2_global_rates 在细分中沿用根轨迹速度与角速度，避免反复从近等端点相减。不更改原面积容差、18层深度或4095节点预算。

新证据 adaptive_global_rates_001 两档仍失败且已结束：128档子区间0.83592987060546875:0.83593368530273438，closure 5.118e-18、difference 3.441e-17m²；256档0.5792388916015625:0.57924270629882812，closure 1.688e-19、difference 2.874e-17m²。故不能将递归重算速度宣称为唯一根因或已修复。已把失败子区间写入异常，后续需追踪具体工具格-墙格差异及裁剪/网格交叉处的误差来源。不能仅增加深度/容差凑通过。

## 独立v8压力接口已实现，但未通过集成验收

新增 BoundaryPressure，版本v10r1.area_pressure_candidate.v8_adaptive_boundary。将自适应边界与有限工具库存交换接入既有SplitPressure的压力、余料堆、内部通量、重力流失及抬刀接口。保留原压力等本构；不自动改变数据含义。当前明确仅支持标准100×100墙面、6×24工具、5mm网格，且拒绝缺失的边界接收格，不做端点补料。

boundary_pressure_001：
- 空墙+工具2mm材料，3步微小平移/旋转、2N、pitch0.2rad、最后lift0.5，完成，账本误差2.541e-15mL。
- 墙面已有2mm材料：第一步contact后墙格出现-4.49986e-22m³残值，随后联合交换严格非负校验拒绝。证据显示是初始拾料/浮点残值接口问题，尚未修复；不静默裁剪后宣布通过。

73项组件测试通过（12.081秒）只表示这些组件回归通过，不能覆盖上述真实集成失败。v8未冻结，更没有完整G0通过。

## 后续

先定位自适应误差中具体稀疏项与舍入界；同时修复初始拾料的逐格供体限额和账本，保持联合非负守恒。之后扩展v8已有材料墙面的完整压力/抬刀多分辨率案例，再跑完整门槛。正式RL、连续机器人执行器、GPU环境、评估回放仍缺失。

原expanded_split_001：100单段+35长序列已有记录，长序列6失败。该审计管理器33596仍保留，未重复启动。新组合诊断已结束，没有需要续等的新诊断进程。
