# v5 工具库存误差控制候选（L1，尚未通过准入）

本次为软件会话。v4 扩大审计 PID33596 继续运行：100单段完成、69通过，首12条长序列约47–67操作，尚无完整序列结束。没有重启或修改其物理实现。

## 新增候选
adaptive_inventory.py 以独立包装器调用未修改的 SplitPressure，physics_version=v10r1.area_pressure_candidate.v5_inventory_adaptive。每个区间从同一状态计算一次整步及两次半步；比较当前墙面、刀面、刀前余料堆、预计抬刀后墙面，以及流失库存差异。接受两半步结果，不作可能产生负库存的外推。

局部允许误差按位移加旋转外缘扫过距离分配：budget_mm × interval_distance / 0.26m。这是候选局部控制规则，不是经过证明的全局误差界；刚性压力/拓扑切换仍可能放大误差。接受结果仍必须通过完整固定门槛的独立对照。

超差则递归二分；达到深度14或剩余试算预算则明确失败，并保持该输入区间前状态与统计不变。完全相同的姿态和控制输入作为恒等操作。初版测试曾因零位移几何舍入进入无意义细分而失败，修正恒等输入处理后通过；这是新v5的改动，不触碰运行v4。未删余料、未取消抬刀、未裁剪旋转动作或放宽G0。

## 测试与诊断
41项组件回归通过（5.901秒），新增工具潜在误差检测、相同姿态/小位移守恒、失败区间原子回滚3项。不是机器人训练证据。

已启动 adaptive_diagnostic：launcher33936，实际Python PID6180。输出 runs/adaptive_inventory_001；case13原始配方，27个外部分段节点，两档局部预算0.01/0.005mm，各最多20000试算。按段保存进度、接受/拒绝统计、最终库存与耗时；尚无最终结果。诊断进程独立且没有新增5秒监测器。预算耗尽必须记录失败，不能宣称门槛通过。

## 限制与后续
深拷贝、step-doubling与预计抬刀投影开销很大；先检验能否控制误差，再谈编译/批量效率，不把该候选说成提速方案。局部接受不能代替100单段/50长序列，也不能代替完整环境G0与机器人执行G1。正式冻结、完整GPU环境、BC/开发/正式PPO、评估和原生回放均未完成。

下一次先检查两个现有进程和新诊断results/summary，勿重复启动；失败则分析预算或数值不收敛证据，成功也须扩大验证后才能采用。
