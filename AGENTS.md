# 工作区约定与 Agent 工作流

本文件约束本仓库中所有 Agent 会话的行为。每次会话开始时先读本文件。

## 一、基线唯一性

| 目录 | 性质 | 是否开发 |
| --- | --- | --- |
| `D:\VLA\dummy_arm\` | git 管理的开发基线 | 是，唯一 |
| `D:\VLA\_archive\` | 只读留档 | 否 |
| `D:\VLA\career_research_plan\` | 个人规划材料，与本工程分离 | 否，且不进代码仓库 |

2026-09-18 前的 `D:\VLA` 布局、`C:\Users\28017\Documents\ChatGPT\dummy机械臂项目`
以及 `dummy_v2/` 工程均已归档，不再作为开发或发布源。归档中的资料可以引用和抽取，
但不在其中修改。

## 二、硬安全边界

Agent 在任何会话中不得越过以下五条：

1. **不执行任何带 `--enable` 的命令**，不启动 GUI 的跟随、姿态按钮或自动往复，
   不发送任何会驱动实体的串口目标。
2. **不把任何 `*_verified` 从 false 改成 true。** 只能提交带证据路径的待批准
   改动，由用户确认后合入。
3. **不修改 `configs/` 下已标定文件。** 新标定写新文件，记录来源与日期，不覆盖。
4. **不删除或改写 `experiments/` 下任何已有记录**，包括失败与中止日志。
5. **涉及实体运动的步骤，只产出「命令 + 检查清单 + 回滚方式」**，由用户执行。

理由不是形式主义：

- Agent 感知不到物理世界。碰撞、缠线、末端压住东西、工作范围内有人手，
  这些在反馈数字里都看不出来。
- 标定完成前，反馈数字的含义本身未知。符号标反时，错误的编码与错误的解码
  会互相抵消，读数看起来完全正常。第一次点动是唯一能发现符号错误的时刻，
  发现方式是有人用眼睛看着机械臂往哪边转。
- 软件停止不等于物理停止。`serial_backend.stop_commissioning` 失败时只能依赖
  硬件急停；协议返回 ok 只代表命令入队或应答。
- Agent 在会话之间不连续，不记得上次机械臂停在什么姿态、线缆理没理。

## 三、证据分级

每条结论必须标明等级，低等级不得冒充高等级：

| 等级 | 含义 | 例子 |
| --- | --- | --- |
| L1 | 软件测试，不接硬件 | unittest 通过、离线渲染、仿真闭环 |
| L2 | 控制器反馈到位 | 预设复测最大反馈差 0.006° |
| L3 | 实体验收 | 人眼确认姿态、急停实测、外部测量系统 |

「固件说自己到位」和「机器真的在那个位置」是两件事。控制器读数不是末端空间精度。

## 四、会话协议

每次开工固定五步：

1. 读 `README.md`、`docs/STATUS.md`、`docs/plan/` 下当前里程碑的任务卡
2. 声明本次会话类型（见下）
3. 执行
4. 证据写入 `experiments/<日期>_<名称>/`，沿用 `experiments/_template`
5. 更新 `docs/STATUS.md` 对应行，`git commit`

会话类型：

- **软件会话** — Agent 可独立跑完，用户事后审 diff。不连接硬件。
- **硬件会话** — 用户必须在场。Agent 只做前置脚本与事后日志分析，
  中间所有与硬件交互的执行由用户完成。
- **分析会话** — 读日志出结论，不改代码。

## 五、记录原则

- 客观回答，不迎合假设。
- 区分软件测试、控制器反馈与实机验收。
- 原始记录不覆盖，不把未测试平台表述成已验证。
- 失败与中止日志与成功日志同等保留，不能把有日志的测试都算成成功。
- 数据文件的组织规则见 `docs/DATA_FILES.md`。

## 六、当前阶段

里程碑与任务卡在 `docs/plan/`。当前进度以 `docs/STATUS.md` 为准。

实机相关的下一个关键节点是 M3 逐轴标定；在它完成之前，
`configs/hardware.unverified.json` 的门禁会拒绝一切程序化运动，这是设计意图，
不是待修复的缺陷。
